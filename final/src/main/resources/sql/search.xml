<?xml version="1.0" encoding="UTF-8" ?><!--  SQL 맵퍼 파일은 xml이기 때문에 제일 먼저 xml 선언이 옵니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Search">
	
		<select id="lsitCount" resultType="int" parameterType="String">
			select count(*) from apt_complex where complex_address like #{addr}
		</select>
		
		<select id="aptList" resultType="complex" parameterType="java.util.Map">
				
				select * from 
					(select rownum rnum, complex_address, complex_apartname, complex_pdate,complex_state,
										 complex_subway, complex_station, complex_foot
							from
								(select * from apt_complex where complex_address like #{addr} 
									order by complex_pdate asc))
						where rnum &gt; = #{start} and rnum &lt; = #{end}
		</select>
	
	
</mapper>